'use client'
export const OthograthicConfig = {
	numberOrderScale: 100,
	frustumSize: 1,
	clingRadius: 2,
	aspect: window?.innerWidth / window?.innerHeight,
	springConfig: { tension: 270, friction: 26, mass: 0.5 }
}